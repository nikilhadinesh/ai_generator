"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.zodRequiredAny = exports.zodUploadFile = void 0;
exports.zodTransform = zodTransform;
const zod_1 = __importDefault(require("zod"));
const form_data_1 = require("./form-data");
/**
 * Generic function to transform an objects keys from one string to another.
 * If the incoming object does not have a transform key, the original key is retained.
 * If the incoming object has a key that maps to a `null` transform key, then
 * the value is skipped.
 */
function zodTransform(input, transformer) {
    const transformed = {};
    for (const [key, val] of Object.entries(input)) {
        const toKey = transformer[key];
        if (toKey === null) {
            continue;
        }
        transformed[toKey !== null && toKey !== void 0 ? toKey : key] = val;
    }
    return transformed;
}
exports.zodUploadFile = zod_1.default.custom((val) => (0, form_data_1.isUploadFile)(val));
// Workaround because the type zod infers from z.any() allows undefined
// https://github.com/colinhacks/zod/issues/3730
exports.zodRequiredAny = zod_1.default.custom((x) => x !== undefined);
