"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.sleep = exports.RetryConfig = void 0;
class RetryConfig {
    constructor({ base, override, }) {
        var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k;
        this.maxRetries = (_b = (_a = override === null || override === void 0 ? void 0 : override.maxRetries) !== null && _a !== void 0 ? _a : base === null || base === void 0 ? void 0 : base.maxRetries) !== null && _b !== void 0 ? _b : 5;
        this.statusCodes = (_d = (_c = override === null || override === void 0 ? void 0 : override.statusCodes) !== null && _c !== void 0 ? _c : base === null || base === void 0 ? void 0 : base.statusCodes) !== null && _d !== void 0 ? _d : [
            5, // 5XX
            408, // Timeout
            409, // Conflict
            429, // Too Many Requests
        ];
        this.initialDelay = (_f = (_e = override === null || override === void 0 ? void 0 : override.initialDelay) !== null && _e !== void 0 ? _e : base === null || base === void 0 ? void 0 : base.initialDelay) !== null && _f !== void 0 ? _f : 500;
        this.maxDelay = (_h = (_g = override === null || override === void 0 ? void 0 : override.maxDelay) !== null && _g !== void 0 ? _g : base === null || base === void 0 ? void 0 : base.maxDelay) !== null && _h !== void 0 ? _h : 10000;
        this.backoffFactor = (_k = (_j = override === null || override === void 0 ? void 0 : override.backoffFactor) !== null && _j !== void 0 ? _j : base === null || base === void 0 ? void 0 : base.backoffFactor) !== null && _k !== void 0 ? _k : 2.0;
    }
    /**
     * Custom status code comparison to support exact match and
     * range matches
     */
    matchesCode({ statusCode, retryCode, }) {
        if (retryCode < 6) {
            // range check (e.g. 4 means 400-499)
            return (retryCode * 100 <= statusCode && statusCode < (retryCode + 1) * 100);
        }
        else {
            // exact match
            return statusCode === retryCode;
        }
    }
    /**
     * Checks if a retry is allowed according to the config
     */
    shouldRetry({ attempt, statusCode, }) {
        return (attempt <= this.maxRetries &&
            this.statusCodes.some((retryCode) => this.matchesCode({ statusCode, retryCode })));
    }
    /**
     * Calculates the time (ms) the retrier should wait before the
     * next attempt according to the config
     */
    calcNextDelay({ currDelay }) {
        return Math.min(this.maxDelay, currDelay * this.backoffFactor);
    }
}
exports.RetryConfig = RetryConfig;
const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));
exports.sleep = sleep;
