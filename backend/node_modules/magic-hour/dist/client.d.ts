import { CoreClient, ResourceClientOptions } from "make-api-request-js";
import { Environment } from "./environment";
import { V1Client } from "./resources/v1";
export interface ClientOptions extends ResourceClientOptions {
    baseUrl?: string;
    environment?: Environment;
    timeout?: number;
    lazyLoad?: boolean;
    token?: string;
}
export declare class Client {
    private _v1Lazy?;
    protected _client: CoreClient;
    protected _opts: ResourceClientOptions;
    constructor(opts?: ClientOptions);
    get v1(): V1Client;
}
