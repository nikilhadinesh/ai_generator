"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.downloadFiles = downloadFiles;
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const node_fetch_1 = __importDefault(require("node-fetch"));
const logger_1 = require("../logger");
/**
 * Download files from the given download URLs
 */
function downloadFiles(downloads, downloadDirectory) {
    return __awaiter(this, void 0, void 0, function* () {
        const downloadedPaths = [];
        // Use current working directory if no download directory specified
        const baseDir = downloadDirectory || process.cwd();
        // Ensure the download directory exists
        if (!fs.existsSync(baseDir)) {
            fs.mkdirSync(baseDir, { recursive: true });
        }
        (0, logger_1.getLogger)().debug(`Downloading outputs to ${baseDir}`);
        for (const download of downloads) {
            (0, logger_1.getLogger)().debug(`Downloading output from ${download.url}`);
            try {
                const response = yield (0, node_fetch_1.default)(download.url);
                if (!response.ok) {
                    throw new Error(`Failed to download from ${download.url}: ${response.statusText}`);
                }
                const buffer = yield response.arrayBuffer();
                // Extract filename from URL or generate one
                const urlPath = new URL(download.url).pathname;
                const filename = path.basename(urlPath) || `download_${Date.now()}`;
                const filePath = path.join(baseDir, filename);
                fs.writeFileSync(filePath, Buffer.from(buffer));
                (0, logger_1.getLogger)().debug(`Downloaded output to ${filePath}`);
                downloadedPaths.push(filePath);
            }
            catch (error) {
                (0, logger_1.getLogger)().error(`Error downloading file from ${download.url}: ${error}`);
            }
        }
        return downloadedPaths;
    });
}
