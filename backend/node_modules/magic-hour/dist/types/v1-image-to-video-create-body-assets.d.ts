import * as z from "zod";
/**
 * Provide the assets for image-to-video. Sora 2 only supports images with an aspect ratio of `9:16` or `16:9`.
 */
export type V1ImageToVideoCreateBodyAssets = {
    /**
     * The image to use as the last frame of the video.
     *
     * * **`gemini-omni-1.1`**: Supports 360p, 720p, 1080p, 4k.
     * * **`kling-2.6`**: Supports 1080p.
     * * **`kling-3.0`**: Supports 720p, 1080p, 4k.
     * * **`ltx-2.3`**: Supports 480p, 720p, 1080p.
     * * **`ltx-2.5`**: Supports 480p, 720p, 1080p.
     * * **`minimax-h3`**: Not supported
     * * **`seedance-1.5`**: Supports 480p, 720p, 1080p.
     * * **`seedance-2.0`**: Supports 480p, 720p, 1080p, 4k.
     * * **`seedance-2.0-mini`**: Supports 480p, 720p.
     * * **`seedance-2.5`**: Supports 480p, 720p.
     * * **`sora-2`**: Not supported
     * * **`veo3.1`**: Supports 720p, 1080p. Requires a duration of 8 seconds or less.
     * * **`veo3.1-lite`**: Supports 720p, 1080p. Requires a duration of 8 seconds or less.
     * * **`wan-2.2`**: Not supported
     * * **`wan-3.0`**: Supports 480p, 720p, 1080p.
     *
     */
    endImageFilePath?: string | undefined;
    /**
     * The path of the image file. This value is either
     * - a direct URL to the video file
     * - `file_path` field from the response of the [upload urls API](https://docs.magichour.ai/api-reference/files/generate-asset-upload-urls).
     *
     * See the [file upload guide](https://docs.magichour.ai/api-reference/files/generate-asset-upload-urls#input-file) for details.
     *
     */
    imageFilePath: string;
};
/**
 * @internal
 * V1ImageToVideoCreateBodyAssets without any key transformation, this is what
 * we expect to come in as network data
 */
export type External$V1ImageToVideoCreateBodyAssets = {
    end_image_file_path?: string | undefined;
    image_file_path: string;
};
export declare const Schemas$V1ImageToVideoCreateBodyAssets: {
    in: z.ZodType<V1ImageToVideoCreateBodyAssets, z.ZodTypeDef, unknown>;
    out: z.ZodType<External$V1ImageToVideoCreateBodyAssets, z.ZodTypeDef, V1ImageToVideoCreateBodyAssets>;
};
