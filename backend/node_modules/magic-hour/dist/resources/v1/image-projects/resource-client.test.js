"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const magic_hour_1 = __importDefault(require("../../../index.js"));
const node_1 = require("msw/node");
const msw_1 = require("msw");
// Create MSW server
const server = (0, node_1.setupServer)();
// Mock the sleep helper
jest.mock("magic-hour/helpers/sleep", () => ({
    sleep: jest.fn().mockResolvedValue(undefined),
}));
// Mock the download helper
jest.mock("magic-hour/helpers/download", () => ({
    downloadFiles: jest.fn(),
}));
const { sleep } = require("../../../helpers/sleep");
const { downloadFiles } = require("../../../helpers/download");
describe("ImageProjectsClient.checkResult", () => {
    let client;
    beforeAll(() => {
        // Start MSW server
        server.listen();
    });
    afterAll(() => {
        // Stop MSW server
        server.close();
    });
    beforeEach(() => {
        client = new magic_hour_1.default({
            token: "API_TOKEN",
        });
        // Reset all mocks
        jest.clearAllMocks();
        // Reset MSW handlers
        server.resetHandlers();
    });
    describe("Basic functionality", () => {
        test("should return response without waiting when waitForCompletion is false", () => __awaiter(void 0, void 0, void 0, function* () {
            const mockResponse = {
                id: "project-123",
                status: "rendering",
                created_at: "2024-01-01T00:00:00Z",
                credits_charged: 10,
                downloads: [],
                enabled: true,
                error: null,
                image_count: 1,
                name: "Test Project",
                total_frame_cost: 10,
                type: "AI_IMAGE",
            };
            // Set up MSW handler for the API call
            server.use(msw_1.http.get("https://api.magichour.ai/v1/image-projects/project-123", () => {
                return msw_1.HttpResponse.json(mockResponse);
            }));
            const result = yield client.v1.imageProjects.checkResult({ id: "project-123" }, { waitForCompletion: false });
            expect(result).toEqual({
                id: "project-123",
                status: "rendering",
                createdAt: "2024-01-01T00:00:00Z",
                creditsCharged: 10,
                downloads: [],
                enabled: true,
                error: null,
                imageCount: 1,
                name: "Test Project",
                totalFrameCost: 10,
                type: "AI_IMAGE",
            });
            expect(sleep).not.toHaveBeenCalled();
        }));
        test("should wait for completion and return successful result", () => __awaiter(void 0, void 0, void 0, function* () {
            const renderingResponse = {
                id: "project-123",
                status: "rendering",
                created_at: "2024-01-01T00:00:00Z",
                credits_charged: 10,
                downloads: [],
                enabled: true,
                error: null,
                image_count: 1,
                name: "Test Project",
                total_frame_cost: 10,
                type: "AI_IMAGE",
            };
            const completeResponse = {
                id: "project-123",
                status: "complete",
                created_at: "2024-01-01T00:00:00Z",
                credits_charged: 10,
                downloads: [
                    {
                        url: "https://example.com/download/image1.jpg",
                        expires_at: "2024-01-01T01:00:00Z",
                    },
                ],
                enabled: true,
                error: null,
                image_count: 1,
                name: "Test Project",
                total_frame_cost: 10,
                type: "AI_IMAGE",
            };
            let callCount = 0;
            // Set up MSW handler that returns different responses on each call
            server.use(msw_1.http.get("https://api.magichour.ai/v1/image-projects/project-123", () => {
                callCount++;
                if (callCount === 1) {
                    return msw_1.HttpResponse.json(renderingResponse);
                }
                else {
                    return msw_1.HttpResponse.json(completeResponse);
                }
            }));
            downloadFiles.mockResolvedValue(["/tmp/image1.jpg"]);
            const result = yield client.v1.imageProjects.checkResult({ id: "project-123" }, {
                waitForCompletion: true,
                downloadOutputs: true,
                downloadDirectory: "/tmp",
            });
            expect(result).toEqual({
                id: "project-123",
                status: "complete",
                createdAt: "2024-01-01T00:00:00Z",
                creditsCharged: 10,
                downloads: [
                    {
                        url: "https://example.com/download/image1.jpg",
                        expiresAt: "2024-01-01T01:00:00Z",
                    },
                ],
                enabled: true,
                error: null,
                imageCount: 1,
                name: "Test Project",
                totalFrameCost: 10,
                type: "AI_IMAGE",
                downloadedPaths: ["/tmp/image1.jpg"],
            });
            expect(sleep).toHaveBeenCalledWith(500); // Default poll interval
            expect(downloadFiles).toHaveBeenCalledWith([
                {
                    url: "https://example.com/download/image1.jpg",
                    expiresAt: "2024-01-01T01:00:00Z",
                },
            ], "/tmp");
        }));
        test("should handle error status during polling", () => __awaiter(void 0, void 0, void 0, function* () {
            const renderingResponse = {
                id: "project-123",
                status: "rendering",
                created_at: "2024-01-01T00:00:00Z",
                credits_charged: 10,
                downloads: [],
                enabled: true,
                error: null,
                image_count: 1,
                name: "Test Project",
                total_frame_cost: 10,
                type: "AI_IMAGE",
            };
            const errorResponse = {
                id: "project-123",
                status: "error",
                created_at: "2024-01-01T00:00:00Z",
                credits_charged: 10,
                downloads: [],
                enabled: true,
                error: { message: "Rendering failed", code: "ERROR_001" },
                image_count: 1,
                name: "Test Project",
                total_frame_cost: 10,
                type: "AI_IMAGE",
            };
            let callCount = 0;
            // Set up MSW handler that returns different responses on each call
            server.use(msw_1.http.get("https://api.magichour.ai/v1/image-projects/project-123", () => {
                callCount++;
                if (callCount === 1) {
                    return msw_1.HttpResponse.json(renderingResponse);
                }
                else {
                    return msw_1.HttpResponse.json(errorResponse);
                }
            }));
            const result = yield client.v1.imageProjects.checkResult({ id: "project-123" }, { waitForCompletion: true });
            expect(result).toEqual({
                id: "project-123",
                status: "error",
                createdAt: "2024-01-01T00:00:00Z",
                creditsCharged: 10,
                downloads: [],
                enabled: true,
                error: { message: "Rendering failed", code: "ERROR_001" },
                imageCount: 1,
                name: "Test Project",
                totalFrameCost: 10,
                type: "AI_IMAGE",
            });
        }));
        test("should handle canceled status during polling", () => __awaiter(void 0, void 0, void 0, function* () {
            const renderingResponse = {
                id: "project-123",
                status: "rendering",
                created_at: "2024-01-01T00:00:00Z",
                credits_charged: 10,
                downloads: [],
                enabled: true,
                error: null,
                image_count: 1,
                name: "Test Project",
                total_frame_cost: 10,
                type: "AI_IMAGE",
            };
            const canceledResponse = {
                id: "project-123",
                status: "canceled",
                created_at: "2024-01-01T00:00:00Z",
                credits_charged: 10,
                downloads: [],
                enabled: true,
                error: null,
                image_count: 1,
                name: "Test Project",
                total_frame_cost: 10,
                type: "AI_IMAGE",
            };
            let callCount = 0;
            // Set up MSW handler that returns different responses on each call
            server.use(msw_1.http.get("https://api.magichour.ai/v1/image-projects/project-123", () => {
                callCount++;
                if (callCount === 1) {
                    return msw_1.HttpResponse.json(renderingResponse);
                }
                else {
                    return msw_1.HttpResponse.json(canceledResponse);
                }
            }));
            const result = yield client.v1.imageProjects.checkResult({ id: "project-123" }, { waitForCompletion: true });
            expect(result).toEqual({
                id: "project-123",
                status: "canceled",
                createdAt: "2024-01-01T00:00:00Z",
                creditsCharged: 10,
                downloads: [],
                enabled: true,
                error: null,
                imageCount: 1,
                name: "Test Project",
                totalFrameCost: 10,
                type: "AI_IMAGE",
            });
        }));
    });
    describe("Polling behavior", () => {
        test("should use custom poll interval from environment", () => __awaiter(void 0, void 0, void 0, function* () {
            const originalEnv = process.env["MAGIC_HOUR_POLL_INTERVAL"];
            const renderingResponse = {
                id: "project-123",
                status: "rendering",
                created_at: "2024-01-01T00:00:00Z",
                credits_charged: 10,
                downloads: [],
                enabled: true,
                error: null,
                image_count: 1,
                name: "Test Project",
                total_frame_cost: 10,
                type: "AI_IMAGE",
            };
            const completeResponse = {
                id: "project-123",
                status: "complete",
                created_at: "2024-01-01T00:00:00Z",
                credits_charged: 10,
                downloads: [],
                enabled: true,
                error: null,
                image_count: 1,
                name: "Test Project",
                total_frame_cost: 10,
                type: "AI_IMAGE",
            };
            let callCount = 0;
            // Set up MSW handler that returns different responses on each call
            server.use(msw_1.http.get("https://api.magichour.ai/v1/image-projects/project-123", () => {
                callCount++;
                if (callCount === 1) {
                    return msw_1.HttpResponse.json(renderingResponse);
                }
                else {
                    return msw_1.HttpResponse.json(completeResponse);
                }
            }));
            // Set environment variable before the checkResult call
            process.env["MAGIC_HOUR_POLL_INTERVAL"] = "1.5";
            yield client.v1.imageProjects.checkResult({ id: "project-123" });
            expect(sleep).toHaveBeenCalledWith(1500); // 1.5 seconds converted to milliseconds
            // Restore environment variable
            if (originalEnv !== undefined) {
                process.env["MAGIC_HOUR_POLL_INTERVAL"] = originalEnv;
            }
            else {
                delete process.env["MAGIC_HOUR_POLL_INTERVAL"];
            }
        }));
        test("should poll multiple times until completion", () => __awaiter(void 0, void 0, void 0, function* () {
            const renderingResponse = {
                id: "project-123",
                status: "rendering",
                createdAt: "2024-01-01T00:00:00Z",
                creditsCharged: 10,
                downloads: [],
                enabled: true,
                error: null,
                imageCount: 1,
                name: "Test Project",
                totalFrameCost: 10,
                type: "AI_IMAGE",
            };
            const queuedResponse = Object.assign(Object.assign({}, renderingResponse), { status: "queued" });
            const completeResponse = Object.assign(Object.assign({}, renderingResponse), { status: "complete" });
            const getMock = jest.spyOn(client.v1.imageProjects, "get");
            getMock
                .mockResolvedValueOnce(renderingResponse)
                .mockResolvedValueOnce(queuedResponse)
                .mockResolvedValueOnce(completeResponse);
            yield client.v1.imageProjects.checkResult({ id: "project-123" });
            expect(sleep).toHaveBeenCalledTimes(2);
            expect(client.v1.imageProjects.get).toHaveBeenCalledTimes(3);
        }));
    });
    describe("Download functionality", () => {
        test("should skip download when downloadOutputs is false", () => __awaiter(void 0, void 0, void 0, function* () {
            const completeResponse = {
                id: "project-123",
                status: "complete",
                createdAt: "2024-01-01T00:00:00Z",
                creditsCharged: 10,
                downloads: [
                    {
                        url: "https://example.com/download/image1.jpg",
                        expiresAt: "2024-01-01T01:00:00Z",
                    },
                ],
                enabled: true,
                error: null,
                imageCount: 1,
                name: "Test Project",
                totalFrameCost: 10,
                type: "AI_IMAGE",
            };
            jest
                .spyOn(client.v1.imageProjects, "get")
                .mockResolvedValue(completeResponse);
            const result = yield client.v1.imageProjects.checkResult({ id: "project-123" }, {
                waitForCompletion: true,
                downloadOutputs: false,
            });
            expect(result).toEqual(completeResponse);
            expect(downloadFiles).not.toHaveBeenCalled();
        }));
        test("should download files to current directory when no downloadDirectory specified", () => __awaiter(void 0, void 0, void 0, function* () {
            const completeResponse = {
                id: "project-123",
                status: "complete",
                createdAt: "2024-01-01T00:00:00Z",
                creditsCharged: 10,
                downloads: [
                    {
                        url: "https://example.com/download/image1.jpg",
                        expiresAt: "2024-01-01T01:00:00Z",
                    },
                ],
                enabled: true,
                error: null,
                imageCount: 1,
                name: "Test Project",
                totalFrameCost: 10,
                type: "AI_IMAGE",
            };
            jest
                .spyOn(client.v1.imageProjects, "get")
                .mockResolvedValue(completeResponse);
            downloadFiles.mockResolvedValue(["/current/dir/image1.jpg"]);
            const result = yield client.v1.imageProjects.checkResult({ id: "project-123" }, { downloadOutputs: true });
            expect(downloadFiles).toHaveBeenCalledWith(completeResponse.downloads, undefined);
            expect(result.downloadedPaths).toEqual(["/current/dir/image1.jpg"]);
        }));
        test("should handle download errors gracefully", () => __awaiter(void 0, void 0, void 0, function* () {
            const completeResponse = {
                id: "project-123",
                status: "complete",
                createdAt: "2024-01-01T00:00:00Z",
                creditsCharged: 10,
                downloads: [
                    {
                        url: "https://example.com/download/image1.jpg",
                        expiresAt: "2024-01-01T01:00:00Z",
                    },
                ],
                enabled: true,
                error: null,
                imageCount: 1,
                name: "Test Project",
                totalFrameCost: 10,
                type: "AI_IMAGE",
            };
            jest
                .spyOn(client.v1.imageProjects, "get")
                .mockResolvedValue(completeResponse);
            downloadFiles.mockRejectedValue(new Error("Download failed"));
            yield expect(client.v1.imageProjects.checkResult({ id: "project-123" }, { downloadOutputs: true })).rejects.toThrow("Download failed");
        }));
    });
    describe("Default behavior", () => {
        test("should use default options when no options provided", () => __awaiter(void 0, void 0, void 0, function* () {
            const completeResponse = {
                id: "project-123",
                status: "complete",
                createdAt: "2024-01-01T00:00:00Z",
                creditsCharged: 10,
                downloads: [
                    {
                        url: "https://example.com/download/image1.jpg",
                        expiresAt: "2024-01-01T01:00:00Z",
                    },
                ],
                enabled: true,
                error: null,
                imageCount: 1,
                name: "Test Project",
                totalFrameCost: 10,
                type: "AI_IMAGE",
            };
            const renderingResponse = Object.assign(Object.assign({}, completeResponse), { status: "rendering" });
            const getMock = jest.spyOn(client.v1.imageProjects, "get");
            getMock
                .mockResolvedValueOnce(renderingResponse)
                .mockResolvedValueOnce(completeResponse);
            downloadFiles.mockResolvedValue(["/tmp/image1.jpg"]);
            const result = yield client.v1.imageProjects.checkResult({
                id: "project-123",
            });
            expect(result.downloadedPaths).toEqual(["/tmp/image1.jpg"]);
            expect(sleep).toHaveBeenCalledWith(500); // Default poll interval
        }));
    });
    describe("Request options", () => {
        test("should pass additional request options to get method", () => __awaiter(void 0, void 0, void 0, function* () {
            const mockResponse = {
                id: "project-123",
                status: "complete",
                createdAt: "2024-01-01T00:00:00Z",
                creditsCharged: 10,
                downloads: [],
                enabled: true,
                error: null,
                imageCount: 1,
                name: "Test Project",
                totalFrameCost: 10,
                type: "AI_IMAGE",
            };
            const getMock = jest.spyOn(client.v1.imageProjects, "get");
            getMock.mockResolvedValue(mockResponse);
            yield client.v1.imageProjects.checkResult({ id: "project-123" }, {
                waitForCompletion: false,
                timeout: 30000,
            });
            expect(getMock).toHaveBeenCalledWith({ id: "project-123" }, {
                timeout: 30000,
            });
        }));
    });
    describe("Edge cases", () => {
        test("should handle immediate completion without polling", () => __awaiter(void 0, void 0, void 0, function* () {
            const completeResponse = {
                id: "project-123",
                status: "complete",
                createdAt: "2024-01-01T00:00:00Z",
                creditsCharged: 10,
                downloads: [],
                enabled: true,
                error: null,
                imageCount: 1,
                name: "Test Project",
                totalFrameCost: 10,
                type: "AI_IMAGE",
            };
            jest
                .spyOn(client.v1.imageProjects, "get")
                .mockResolvedValue(completeResponse);
            const result = yield client.v1.imageProjects.checkResult({ id: "project-123" }, { waitForCompletion: false });
            expect(result).toEqual(completeResponse);
            expect(sleep).not.toHaveBeenCalled();
            expect(downloadFiles).not.toHaveBeenCalled();
        }));
        test("should handle empty downloads array", () => __awaiter(void 0, void 0, void 0, function* () {
            const completeResponse = {
                id: "project-123",
                status: "complete",
                createdAt: "2024-01-01T00:00:00Z",
                creditsCharged: 10,
                downloads: [],
                enabled: true,
                error: null,
                imageCount: 1,
                name: "Test Project",
                totalFrameCost: 10,
                type: "AI_IMAGE",
            };
            jest
                .spyOn(client.v1.imageProjects, "get")
                .mockResolvedValue(completeResponse);
            downloadFiles.mockResolvedValue([]);
            const result = yield client.v1.imageProjects.checkResult({ id: "project-123" }, { downloadOutputs: true });
            expect(result.downloadedPaths).toEqual([]);
            expect(downloadFiles).toHaveBeenCalledWith([], undefined);
        }));
        test("should handle get method throwing error", () => __awaiter(void 0, void 0, void 0, function* () {
            const error = new Error("API Error");
            jest.spyOn(client.v1.imageProjects, "get").mockRejectedValue(error);
            yield expect(client.v1.imageProjects.checkResult({ id: "project-123" })).rejects.toThrow("API Error");
        }));
    });
});
