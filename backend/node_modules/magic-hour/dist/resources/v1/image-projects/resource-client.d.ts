import { ApiPromise, CoreClient, CoreResourceClient, RequestOptions, ResourceClientOptions } from "make-api-request-js";
import { types } from "../../../index.js";
import { GenerateOptions } from "../../../helpers/generate-type";
import * as requests from "../../../resources/v1/image-projects/request-types";
/**
 * Extended response interface that includes downloaded paths
 */
export interface V1ImageProjectsGetResponseWithDownloads extends types.V1ImageProjectsGetResponse {
    /**
     * The paths to the downloaded files.
     * This field is only populated if `download_outputs` is True and the image project is complete.
     */
    downloadedPaths?: string[];
}
export declare class ImageProjectsClient extends CoreResourceClient {
    constructor(coreClient: CoreClient, opts: ResourceClientOptions);
    /**
     * Check the result of an image project with optional waiting and downloading.
     *
     * This method retrieves the status of an image project and optionally waits for completion
     * and downloads the output files.
     *
     * @param request - Request object containing the image project ID
     * @param opts - Additional request options
     * @returns The image project response with optional downloaded file paths included
     */
    checkResult(request: requests.GetRequest, opts?: GenerateOptions): Promise<V1ImageProjectsGetResponseWithDownloads>;
    /**
     * Delete image
     *
     * Permanently delete the rendered image(s). This action is not reversible, please be sure before deleting.
     *
     * DELETE /v1/image-projects/{id}
     */
    delete(request: requests.DeleteRequest, opts?: RequestOptions): ApiPromise<null>;
    /**
     * Get image details
     *
     * Check the progress of a image project. The `downloads` field is populated after a successful render.
     *
     * **Statuses**
     * - `queued` — waiting to start
     * - `rendering` — in progress
     * - `complete` — ready; see `downloads`
     * - `error` — a failure occurred (see `error`)
     * - `canceled` — user canceled
     * - `draft` — not used
     *
     * GET /v1/image-projects/{id}
     */
    get(request: requests.GetRequest, opts?: RequestOptions): ApiPromise<types.V1ImageProjectsGetResponse>;
}
