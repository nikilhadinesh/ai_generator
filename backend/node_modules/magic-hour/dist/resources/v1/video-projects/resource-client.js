"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.VideoProjectsClient = void 0;
const make_api_request_js_1 = require("make-api-request-js");
const download_1 = require("../../../helpers/download");
const sleep_1 = require("../../../helpers/sleep");
const logger_1 = require("../../../logger");
const v1_video_projects_get_response_1 = require("../../../types/v1-video-projects-get-response");
class VideoProjectsClient extends make_api_request_js_1.CoreResourceClient {
    constructor(coreClient, opts) {
        super(coreClient, opts);
    }
    /**
     * Check the result of a video project with optional waiting and downloading.
     *
     * This method retrieves the status of a video project and optionally waits for completion
     * and downloads the output files.
     *
     * @param request - Request object containing the video project ID
     * @param opts - Additional request options
     * @returns The video project response with optional downloaded file paths included
     */
    checkResult(request, opts) {
        return __awaiter(this, void 0, void 0, function* () {
            var _a, _b;
            const _c = opts || {}, { waitForCompletion = true, downloadOutputs = true, downloadDirectory = undefined } = _c, requestOpts = __rest(_c, ["waitForCompletion", "downloadOutputs", "downloadDirectory"]);
            let apiResponse = yield this.get({ id: request.id }, requestOpts);
            if (!waitForCompletion) {
                return Object.assign({}, apiResponse);
            }
            const pollInterval = parseFloat(process.env["MAGIC_HOUR_POLL_INTERVAL"] || "0.5");
            (0, logger_1.getLogger)().debug(`Polling video project ${request.id} every ${pollInterval} seconds`);
            while (!["complete", "error", "canceled"].includes(apiResponse.status)) {
                yield (0, sleep_1.sleep)(pollInterval * 1000); // Convert seconds to milliseconds
                (0, logger_1.getLogger)().info(`Video project ${request.id} status: ${apiResponse.status}, waiting for ${pollInterval} seconds and checking again`);
                apiResponse = yield this.get({ id: request.id }, requestOpts);
            }
            if (apiResponse.status !== "complete") {
                if (apiResponse.status === "error") {
                    const message = `Video project ${request.id} has status ${apiResponse.status}: [${(_a = apiResponse.error) === null || _a === void 0 ? void 0 : _a.code}] ${(_b = apiResponse.error) === null || _b === void 0 ? void 0 : _b.message}`;
                    (0, logger_1.getLogger)().error(message);
                }
                else {
                    (0, logger_1.getLogger)().info(`Video project ${request.id} has status ${apiResponse.status}. Stopping polling.`);
                }
                return Object.assign({}, apiResponse);
            }
            if (!downloadOutputs) {
                (0, logger_1.getLogger)().info(`Download outputs is disabled. Returning video project ${request.id} with status ${apiResponse.status}`);
                return Object.assign({}, apiResponse);
            }
            (0, logger_1.getLogger)().debug(`Downloading outputs for video project ${request.id} to ${downloadDirectory !== null && downloadDirectory !== void 0 ? downloadDirectory : "current directory"}`);
            const downloadedPaths = yield (0, download_1.downloadFiles)(apiResponse.downloads, downloadDirectory);
            (0, logger_1.getLogger)().info(`Downloaded outputs for video project ${request.id} to ${downloadedPaths}`);
            return Object.assign(Object.assign({}, apiResponse), { downloadedPaths });
        });
    }
    /**
     * Delete video
     *
     * Permanently delete the rendered video. This action is not reversible, please be sure before deleting.
     *
     * DELETE /v1/video-projects/{id}
     */
    delete(request, opts) {
        return this._client.makeRequest({
            method: "delete",
            path: `/v1/video-projects/${request.id}`,
            auth: ["bearerAuth"],
            opts,
        });
    }
    /**
     * Get video details
     *
     * Check the progress of a video project. The `downloads` field is populated after a successful render.
     *
     * **Statuses**
     * - `queued` — waiting to start
     * - `rendering` — in progress
     * - `complete` — ready; see `downloads`
     * - `error` — a failure occurred (see `error`)
     * - `canceled` — user canceled
     * - `draft` — not used
     *
     * GET /v1/video-projects/{id}
     */
    get(request, opts) {
        return this._client.makeRequest({
            method: "get",
            path: `/v1/video-projects/${request.id}`,
            auth: ["bearerAuth"],
            responseSchema: v1_video_projects_get_response_1.Schemas$V1VideoProjectsGetResponse.in,
            opts,
        });
    }
}
exports.VideoProjectsClient = VideoProjectsClient;
