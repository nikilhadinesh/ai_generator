import { ApiPromise, CoreClient, CoreResourceClient, RequestOptions, ResourceClientOptions } from "make-api-request-js";
import { types } from "../../../index.js";
import { GenerateOptions } from "../../../helpers/generate-type";
import * as requests from "../../../resources/v1/video-projects/request-types";
/**
 * Extended response interface that includes downloaded paths
 */
export interface V1VideoProjectsGetResponseWithDownloads extends types.V1VideoProjectsGetResponse {
    /**
     * The paths to the downloaded files.
     * This field is only populated if `download_outputs` is True and the video project is complete.
     */
    downloadedPaths?: string[];
}
export declare class VideoProjectsClient extends CoreResourceClient {
    constructor(coreClient: CoreClient, opts: ResourceClientOptions);
    /**
     * Check the result of a video project with optional waiting and downloading.
     *
     * This method retrieves the status of a video project and optionally waits for completion
     * and downloads the output files.
     *
     * @param request - Request object containing the video project ID
     * @param opts - Additional request options
     * @returns The video project response with optional downloaded file paths included
     */
    checkResult(request: requests.GetRequest, opts?: GenerateOptions): Promise<V1VideoProjectsGetResponseWithDownloads>;
    /**
     * Delete video
     *
     * Permanently delete the rendered video. This action is not reversible, please be sure before deleting.
     *
     * DELETE /v1/video-projects/{id}
     */
    delete(request: requests.DeleteRequest, opts?: RequestOptions): ApiPromise<null>;
    /**
     * Get video details
     *
     * Check the progress of a video project. The `downloads` field is populated after a successful render.
     *
     * **Statuses**
     * - `queued` — waiting to start
     * - `rendering` — in progress
     * - `complete` — ready; see `downloads`
     * - `error` — a failure occurred (see `error`)
     * - `canceled` — user canceled
     * - `draft` — not used
     *
     * GET /v1/video-projects/{id}
     */
    get(request: requests.GetRequest, opts?: RequestOptions): ApiPromise<types.V1VideoProjectsGetResponse>;
}
