import { ApiPromise, CoreClient, CoreResourceClient, RequestOptions, ResourceClientOptions } from "make-api-request-js";
import { types } from "../../../index.js";
import { GenerateOptions, GenerateRequestType } from "../../../helpers/generate-type";
import * as requests from "../../../resources/v1/text-to-video/request-types";
type GenerateRequest = GenerateRequestType<requests.CreateRequest, {}>;
export declare class TextToVideoClient extends CoreResourceClient {
    constructor(coreClient: CoreClient, opts: ResourceClientOptions);
    /**
     * Text-to-Video
     *
     * Create a Text To Video video
     *
     * This method provides a convenient way to create a request and automatically wait for completion and download outputs.
     *
     * @example
     * ```typescript
     * import { Client } from "../../../index.js";
     *
     * const client = new Client({ token: process.env["API_TOKEN"]!! });
     * const res = await client.v1.textToVideo.generate(
     *   {
     *     endSeconds: 5.0,
     *     name: "Text To Video video",
     *     orientation: "landscape",
     *     resolution: "720p",
     *     style: { prompt: "a dog running" },
     *   },
     *   {
     *     waitForCompletion: true,
     *     downloadOutputs: true,
     *     downloadDirectory: ".",
     *   },
     * );
     * ```
     */
    generate(request: GenerateRequest, opts?: GenerateOptions): Promise<import("../video-projects/resource-client").V1VideoProjectsGetResponseWithDownloads>;
    /**
     * Text-to-Video
     *
     * **What this API does**
     *
     * Create the same Text To Video you can make in the browser, but programmatically, so you can automate it, run it at scale, or connect it to your own app or workflow.
     *
     * **Good for**
     * - Automation and batch processing
     * - Adding text to video into apps, pipelines, or tools
     *
     * **How it works (3 steps)**
     * 1) Upload your inputs (video, image, or audio) with [Generate Upload URLs](https://docs.magichour.ai/api-reference/files/generate-asset-upload-urls) and copy the `file_path`.
     * 2) Send a request to create a text to video job with the basic fields.
     * 3) Check the job status until it's `complete`, then download the result from `downloads`.
     *
     * **Key options**
     * - Inputs: usually a file, sometimes a YouTube link, depending on project type
     * - Resolution: free users are limited to 576px; higher plans unlock HD and larger sizes
     * - Extra fields: e.g. `face_swap_mode`, `start_seconds`/`end_seconds`, or a text prompt
     *
     * **Cost**
     * Credits are only charged for the frames that actually render. You'll see an estimate when the job is queued, and the final total after it's done.
     *
     * For detailed examples, see the [product page](https://magichour.ai/products/text-to-video).
     *
     * POST /v1/text-to-video
     */
    create(request: requests.CreateRequest, opts?: RequestOptions): ApiPromise<types.V1TextToVideoCreateResponse>;
}
export {};
