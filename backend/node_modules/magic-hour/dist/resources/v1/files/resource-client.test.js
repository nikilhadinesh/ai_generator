"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const magic_hour_1 = require("../../../index.js");
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const stream_1 = require("stream");
const msw_1 = require("msw");
const node_1 = require("msw/node");
// Mock fs module
jest.mock("fs", () => ({
    existsSync: jest.fn(),
    readFileSync: jest.fn(),
}));
const mockFs = fs;
// Create MSW server for mocking API requests
const server = (0, node_1.setupServer)(
// Mock upload URLs creation - handle both possible URLs
msw_1.http.post("https://api.magichour.ai/v1/files/upload-urls", () => {
    return msw_1.HttpResponse.json({
        items: [
            {
                upload_url: "https://storage.example.com/upload?token=abc123",
                file_path: "api-assets/12345/image.jpg",
                expires_at: "2024-12-31T23:59:59Z",
            },
        ],
    });
}), 
// Mock file upload to storage
msw_1.http.put("https://storage.example.com/upload", () => {
    return msw_1.HttpResponse.json({ status: 200 });
}), 
// Handle any other upload URLs that might be generated
msw_1.http.put("https://example.com/upload", () => {
    return msw_1.HttpResponse.json({ status: 200 });
}));
// Mock RUNTIME to ensure we use node-fetch
jest.mock("make-api-request-js", () => {
    const actual = jest.requireActual("make-api-request-js");
    return Object.assign(Object.assign({}, actual), { RUNTIME: { type: "node" } });
});
// Start and stop MSW server
beforeAll(() => server.listen());
afterAll(() => server.close());
afterEach(() => server.resetHandlers());
describe("FilesClient.uploadFile", () => {
    let client;
    beforeEach(() => {
        client = new magic_hour_1.Client({
            token: "API_TOKEN",
        });
        // Reset all mocks
        jest.clearAllMocks();
        // Setup default fs mocks
        mockFs.existsSync.mockReturnValue(true);
        mockFs.readFileSync.mockReturnValue(Buffer.from("mock file content"));
    });
    describe("URL and already-uploaded path handling", () => {
        test("should return HTTP URL as-is without uploading", () => __awaiter(void 0, void 0, void 0, function* () {
            const url = "http://example.com/image.jpg";
            const createSpy = jest.spyOn(client.v1.files.uploadUrls, "create");
            const result = yield client.v1.files.uploadFile(url);
            expect(result).toBe(url);
            expect(createSpy).not.toHaveBeenCalled();
            expect(mockFs.existsSync).not.toHaveBeenCalled();
            createSpy.mockRestore();
        }));
        test("should return HTTPS URL as-is without uploading", () => __awaiter(void 0, void 0, void 0, function* () {
            const url = "https://example.com/path/to/video.mp4";
            const createSpy = jest.spyOn(client.v1.files.uploadUrls, "create");
            const result = yield client.v1.files.uploadFile(url);
            expect(result).toBe(url);
            expect(createSpy).not.toHaveBeenCalled();
            expect(mockFs.existsSync).not.toHaveBeenCalled();
            createSpy.mockRestore();
        }));
        test("should return already-uploaded api-assets path as-is without uploading", () => __awaiter(void 0, void 0, void 0, function* () {
            const apiAssetsPath = "api-assets/12345/image.jpg";
            const createSpy = jest.spyOn(client.v1.files.uploadUrls, "create");
            const result = yield client.v1.files.uploadFile(apiAssetsPath);
            expect(result).toBe(apiAssetsPath);
            expect(createSpy).not.toHaveBeenCalled();
            expect(mockFs.existsSync).not.toHaveBeenCalled();
            createSpy.mockRestore();
        }));
        test("should handle URLs with query parameters", () => __awaiter(void 0, void 0, void 0, function* () {
            const url = "https://example.com/image.jpg?token=abc123&expires=12345";
            const createSpy = jest.spyOn(client.v1.files.uploadUrls, "create");
            const result = yield client.v1.files.uploadFile(url);
            expect(result).toBe(url);
            expect(createSpy).not.toHaveBeenCalled();
            createSpy.mockRestore();
        }));
        test("should upload local file paths that look similar to URLs but are not", () => __awaiter(void 0, void 0, void 0, function* () {
            const filePath = "/http/example.com/image.jpg";
            const createSpy = jest.spyOn(client.v1.files.uploadUrls, "create");
            const result = yield client.v1.files.uploadFile(filePath);
            expect(result).toBe("api-assets/12345/image.jpg");
            expect(createSpy).toHaveBeenCalled();
            createSpy.mockRestore();
        }));
    });
    describe("File type detection", () => {
        // MSW handles the uploadUrls.create call automatically
        test("should detect video file types correctly", () => __awaiter(void 0, void 0, void 0, function* () {
            const videoExtensions = ["mp4", "m4v", "mov", "webm"];
            for (const ext of videoExtensions) {
                const filePath = `/path/to/video.${ext}`;
                // Spy on the uploadUrls.create method to verify it gets called with correct params
                const createSpy = jest.spyOn(client.v1.files.uploadUrls, "create");
                const result = yield client.v1.files.uploadFile(filePath);
                expect(createSpy).toHaveBeenCalledWith({
                    items: [{ extension: ext, type: "video" }],
                });
                expect(result).toBe("api-assets/12345/image.jpg"); // MSW returns this mock value
                createSpy.mockRestore();
            }
        }));
        test("should detect audio file types correctly", () => __awaiter(void 0, void 0, void 0, function* () {
            const audioExtensions = ["mp3", "mpeg", "wav", "aac", "aiff", "flac"];
            for (const ext of audioExtensions) {
                const filePath = `/path/to/audio.${ext}`;
                const createSpy = jest.spyOn(client.v1.files.uploadUrls, "create");
                const result = yield client.v1.files.uploadFile(filePath);
                expect(createSpy).toHaveBeenCalledWith({
                    items: [{ extension: ext, type: "audio" }],
                });
                expect(result).toBe("api-assets/12345/image.jpg");
                createSpy.mockRestore();
            }
        }));
        test("should detect image file types correctly", () => __awaiter(void 0, void 0, void 0, function* () {
            const imageExtensions = [
                "png",
                "jpg",
                "jpeg",
                "webp",
                "avif",
                "jp2",
                "tiff",
                "bmp",
            ];
            for (const ext of imageExtensions) {
                const filePath = `/path/to/image.${ext}`;
                const createSpy = jest.spyOn(client.v1.files.uploadUrls, "create");
                const result = yield client.v1.files.uploadFile(filePath);
                expect(createSpy).toHaveBeenCalledWith({
                    items: [{ extension: ext, type: "image" }],
                });
                expect(result).toBe("api-assets/12345/image.jpg");
                createSpy.mockRestore();
            }
        }));
        test("should handle case-insensitive file extensions", () => __awaiter(void 0, void 0, void 0, function* () {
            const testCases = [
                {
                    input: "/path/to/VIDEO.MP4",
                    expectedExt: "mp4",
                    expectedType: "video",
                },
                {
                    input: "/path/to/IMAGE.JPG",
                    expectedExt: "jpg",
                    expectedType: "image",
                },
                {
                    input: "/path/to/AUDIO.WAV",
                    expectedExt: "wav",
                    expectedType: "audio",
                },
            ];
            for (const { input, expectedExt, expectedType } of testCases) {
                const createSpy = jest.spyOn(client.v1.files.uploadUrls, "create");
                const result = yield client.v1.files.uploadFile(input);
                expect(createSpy).toHaveBeenCalledWith({
                    items: [{ extension: expectedExt, type: expectedType }],
                });
                expect(result).toBe("api-assets/12345/image.jpg");
                createSpy.mockRestore();
            }
        }));
        test("should throw error for unsupported file extensions", () => __awaiter(void 0, void 0, void 0, function* () {
            const unsupportedFiles = [
                "/path/to/file.txt",
                "/path/to/document.pdf",
                "/path/to/archive.zip",
                "/path/to/data.json",
            ];
            for (const filePath of unsupportedFiles) {
                const ext = path.extname(filePath).slice(1).toLowerCase();
                yield expect(client.v1.files.uploadFile(filePath)).rejects.toThrow(`Unsupported file extension: ${ext}`);
            }
        }));
    });
    describe("File input validation", () => {
        const mockUploadResponse = {
            items: [
                {
                    uploadUrl: "https://example.com/upload",
                    filePath: "uploaded-file.jpg",
                    expiresAt: "2024-01-01T00:00:00Z",
                },
            ],
        };
        beforeEach(() => {
            jest
                .spyOn(client.v1.files.uploadUrls, "create")
                .mockResolvedValue(mockUploadResponse);
        });
        test("should validate file exists for string input", () => __awaiter(void 0, void 0, void 0, function* () {
            const filePath = "/path/to/image.jpg";
            mockFs.existsSync.mockReturnValue(true);
            yield client.v1.files.uploadFile(filePath);
            expect(mockFs.existsSync).toHaveBeenCalledWith(filePath);
        }));
        test("should throw error for non-existent file path", () => __awaiter(void 0, void 0, void 0, function* () {
            const filePath = "/nonexistent/file.jpg";
            mockFs.existsSync.mockReturnValue(false);
            yield expect(client.v1.files.uploadFile(filePath)).rejects.toThrow(`File not found: ${filePath}`);
        }));
        test("should reject Buffer input without filename", () => __awaiter(void 0, void 0, void 0, function* () {
            const buffer = Buffer.from("image content");
            yield expect(client.v1.files.uploadFile(buffer)).rejects.toThrow("Buffer input requires a file name for extension detection");
        }));
        test("should validate Readable stream has path property", () => __awaiter(void 0, void 0, void 0, function* () {
            const mockStream = new stream_1.Readable({
                read() {
                    this.push(Buffer.from("stream content"));
                    this.push(null);
                },
            });
            // Stream without path property should fail
            yield expect(client.v1.files.uploadFile(mockStream)).rejects.toThrow("Stream must have a 'path' property for extension detection");
        }));
        test("should accept Readable stream with path property", () => __awaiter(void 0, void 0, void 0, function* () {
            const mockStream = new stream_1.Readable({
                read() {
                    this.push(Buffer.from("stream content"));
                    this.push(null);
                },
            });
            mockStream.path = "/path/to/video.mp4";
            const result = yield client.v1.files.uploadFile(mockStream);
            expect(result).toBe("uploaded-file.jpg");
            expect(client.v1.files.uploadUrls.create).toHaveBeenCalledWith({
                items: [{ extension: "mp4", type: "video" }],
            });
        }));
        test.skip("should handle File object in browser-like environment", () => __awaiter(void 0, void 0, void 0, function* () {
            // Skipping this test as File object mocking is complex in Node.js environment
            // This functionality would work in actual browser environments
        }));
    });
    describe("File content processing", () => {
        // MSW handles the uploadUrls.create call automatically
        test("should read file content from file path", () => __awaiter(void 0, void 0, void 0, function* () {
            const filePath = "/path/to/image.jpg";
            const fileContent = Buffer.from("image content");
            mockFs.readFileSync.mockReturnValue(fileContent);
            const createSpy = jest.spyOn(client.v1.files.uploadUrls, "create");
            const result = yield client.v1.files.uploadFile(filePath);
            expect(mockFs.readFileSync).toHaveBeenCalledWith(filePath);
            expect(createSpy).toHaveBeenCalledWith({
                items: [{ extension: "jpg", type: "image" }],
            });
            expect(result).toBe("api-assets/12345/image.jpg");
            createSpy.mockRestore();
        }));
        test("should process Readable stream content", () => __awaiter(void 0, void 0, void 0, function* () {
            const streamContent = Buffer.from("stream video content");
            const mockStream = new stream_1.Readable({
                read() {
                    this.push(streamContent);
                    this.push(null);
                },
            });
            mockStream.path = "/path/to/video.mp4";
            const createSpy = jest.spyOn(client.v1.files.uploadUrls, "create");
            const result = yield client.v1.files.uploadFile(mockStream);
            expect(createSpy).toHaveBeenCalledWith({
                items: [{ extension: "mp4", type: "video" }],
            });
            expect(result).toBe("api-assets/12345/image.jpg");
            createSpy.mockRestore();
        }));
        test("should handle large stream content", () => __awaiter(void 0, void 0, void 0, function* () {
            const largeContent = Buffer.from("a".repeat(1024 * 1024)); // 1MB of content
            const mockStream = new stream_1.Readable({
                read() {
                    this.push(largeContent);
                    this.push(null);
                },
            });
            mockStream.path = "/path/to/large-video.mp4";
            const createSpy = jest.spyOn(client.v1.files.uploadUrls, "create");
            const result = yield client.v1.files.uploadFile(mockStream);
            expect(createSpy).toHaveBeenCalledWith({
                items: [{ extension: "mp4", type: "video" }],
            });
            expect(result).toBe("api-assets/12345/image.jpg");
            createSpy.mockRestore();
        }));
        test.skip("should handle File object arrayBuffer conversion", () => __awaiter(void 0, void 0, void 0, function* () {
            // Skipping this test as File object mocking is complex in Node.js environment
            // This functionality would work in actual browser environments
        }));
    });
    describe("Upload process and error handling", () => {
        test("should successfully complete upload process", () => __awaiter(void 0, void 0, void 0, function* () {
            const createSpy = jest.spyOn(client.v1.files.uploadUrls, "create");
            const result = yield client.v1.files.uploadFile("/path/to/image.jpg");
            expect(result).toBe("api-assets/12345/image.jpg");
            expect(createSpy).toHaveBeenCalledWith({
                items: [{ extension: "jpg", type: "image" }],
            });
            createSpy.mockRestore();
        }));
        test("should handle empty upload URL response", () => __awaiter(void 0, void 0, void 0, function* () {
            const mockResponse = { items: [] };
            jest
                .spyOn(client.v1.files.uploadUrls, "create")
                .mockResolvedValue(mockResponse);
            yield expect(client.v1.files.uploadFile("/path/to/image.jpg")).rejects.toThrow("No upload URL was returned from the server");
        }));
        test("should handle null upload info", () => __awaiter(void 0, void 0, void 0, function* () {
            const mockResponse = { items: [null] };
            jest
                .spyOn(client.v1.files.uploadUrls, "create")
                .mockResolvedValue(mockResponse);
            yield expect(client.v1.files.uploadFile("/path/to/image.jpg")).rejects.toThrow("Upload info is missing from server response");
        }));
        test("should handle upload HTTP failure", () => __awaiter(void 0, void 0, void 0, function* () {
            // Change server behavior for this test
            server.use(msw_1.http.put("https://storage.example.com/upload", () => {
                return msw_1.HttpResponse.json({ error: "Internal Server Error" }, { status: 500 });
            }));
            yield expect(client.v1.files.uploadFile("/path/to/image.jpg")).rejects.toThrow("Upload failed with status 500");
        }));
        test("should handle network errors during upload", () => __awaiter(void 0, void 0, void 0, function* () {
            // Change server behavior for this test to simulate network error
            server.use(msw_1.http.put("https://storage.example.com/upload", () => {
                return msw_1.HttpResponse.error();
            }));
            yield expect(client.v1.files.uploadFile("/path/to/image.jpg")).rejects.toThrow("Network error");
        }));
        test("should handle upload URLs creation failure", () => __awaiter(void 0, void 0, void 0, function* () {
            jest
                .spyOn(client.v1.files.uploadUrls, "create")
                .mockRejectedValue(new Error("API authentication failed"));
            yield expect(client.v1.files.uploadFile("/path/to/image.jpg")).rejects.toThrow("API authentication failed");
        }));
        test("should handle file system read errors", () => __awaiter(void 0, void 0, void 0, function* () {
            mockFs.readFileSync.mockImplementation(() => {
                throw new Error("Permission denied");
            });
            yield expect(client.v1.files.uploadFile("/path/to/image.jpg")).rejects.toThrow("Permission denied");
        }));
    });
    describe("Integration and edge cases", () => {
        test("should handle files with special characters in names", () => __awaiter(void 0, void 0, void 0, function* () {
            const specialFiles = [
                "/path/to/image with spaces.jpg",
                "/path/to/image-with-dashes.png",
                "/path/to/image_with_underscores.webp",
                "/path/to/image.with.dots.mp4",
                "/path/to/123numeric.mp3",
            ];
            const mockResponse = {
                items: [
                    {
                        uploadUrl: "https://example.com/upload",
                        filePath: "uploaded-file.jpg",
                        expiresAt: "2024-01-01T00:00:00Z",
                    },
                ],
            };
            jest
                .spyOn(client.v1.files.uploadUrls, "create")
                .mockResolvedValue(mockResponse);
            for (const filePath of specialFiles) {
                const ext = path.extname(filePath).slice(1).toLowerCase();
                const expectedType = getFileTypeFromExtension(ext);
                yield client.v1.files.uploadFile(filePath);
                expect(client.v1.files.uploadUrls.create).toHaveBeenCalledWith({
                    items: [{ extension: ext, type: expectedType }],
                });
            }
        }));
        test("should handle empty file content", () => __awaiter(void 0, void 0, void 0, function* () {
            mockFs.readFileSync.mockReturnValue(Buffer.from(""));
            const createSpy = jest.spyOn(client.v1.files.uploadUrls, "create");
            const result = yield client.v1.files.uploadFile("/path/to/empty.jpg");
            expect(result).toBe("api-assets/12345/image.jpg");
            expect(createSpy).toHaveBeenCalledWith({
                items: [{ extension: "jpg", type: "image" }],
            });
            createSpy.mockRestore();
        }));
    });
});
// Helper function for tests
function getFileTypeFromExtension(ext) {
    const videoExts = ["mp4", "m4v", "mov", "webm"];
    const audioExts = ["mp3", "mpeg", "wav", "aac", "aiff", "flac"];
    const imageExts = [
        "png",
        "jpg",
        "jpeg",
        "webp",
        "avif",
        "jp2",
        "tiff",
        "bmp",
    ];
    if (videoExts.includes(ext))
        return "video";
    if (audioExts.includes(ext))
        return "audio";
    if (imageExts.includes(ext))
        return "image";
    throw new Error(`Unsupported extension: ${ext}`);
}
