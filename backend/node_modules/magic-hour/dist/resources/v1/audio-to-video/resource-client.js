"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.AudioToVideoClient = void 0;
const make_api_request_js_1 = require("make-api-request-js");
const logger_1 = require("../../../logger");
const files_1 = require("../../../resources/v1/files");
const video_projects_1 = require("../../../resources/v1/video-projects");
const v1_audio_to_video_create_body_1 = require("../../../types/v1-audio-to-video-create-body");
const v1_audio_to_video_create_response_1 = require("../../../types/v1-audio-to-video-create-response");
class AudioToVideoClient extends make_api_request_js_1.CoreResourceClient {
    constructor(coreClient, opts) {
        super(coreClient, opts);
    }
    /**
     * Audio-to-Video
     *
     * Create a Audio To Video video
     *
     * This method provides a convenient way to create a request and automatically wait for completion and download outputs.
     *
     * @example
     * ```typescript
     * import { Client } from "../../../index.js";
     *
     * const client = new Client({ token: process.env["API_TOKEN"]!! });
     * const res = await client.v1.audioToVideo.generate(
     *   {
     *     assets: {
     *       audioFilePath: "/path/to/1234.mp3",
     *       imageFilePath: "/path/to/1234.png",
     *     },
     *     endSeconds: 15.0,
     *     name: "Audio To Video video",
     *     resolution: "720p",
     *     startSeconds: 0.0,
     *   },
     *   {
     *     waitForCompletion: true,
     *     downloadOutputs: true,
     *     downloadDirectory: ".",
     *   },
     * );
     * ```
     */
    generate(request_1) {
        return __awaiter(this, arguments, void 0, function* (request, opts = {}) {
            const { waitForCompletion = true, downloadOutputs = true, downloadDirectory = undefined } = opts, createOpts = __rest(opts, ["waitForCompletion", "downloadOutputs", "downloadDirectory"]);
            const fileClient = new files_1.FilesClient(this._client, this._opts);
            const _a = request.assets, { audioFilePath, imageFilePath } = _a, restAssets = __rest(_a, ["audioFilePath", "imageFilePath"]);
            (0, logger_1.getLogger)().debug(`Uploading file ${audioFilePath} to Magic Hour's storage`);
            if (imageFilePath) {
                (0, logger_1.getLogger)().debug(`Uploading file ${imageFilePath} to Magic Hour's storage`);
            }
            const [uploadedAudioFilePath, uploadedImageFilePath] = yield Promise.all([
                fileClient.uploadFile(audioFilePath),
                imageFilePath
                    ? fileClient.uploadFile(imageFilePath)
                    : Promise.resolve(imageFilePath),
            ]);
            (0, logger_1.getLogger)().info(`Uploaded file ${audioFilePath} to Magic Hour's storage as ${uploadedAudioFilePath}`);
            if (imageFilePath) {
                (0, logger_1.getLogger)().info(`Uploaded file ${imageFilePath} to Magic Hour's storage as ${uploadedImageFilePath}`);
            }
            const createResponse = yield this.create(Object.assign(Object.assign({}, request), { assets: Object.assign(Object.assign({}, restAssets), { audioFilePath: uploadedAudioFilePath, imageFilePath: imageFilePath ? uploadedImageFilePath : imageFilePath }) }), createOpts);
            (0, logger_1.getLogger)().info(`Created AudioToVideoClient project ${createResponse.id}`);
            const projectsClient = new video_projects_1.VideoProjectsClient(this._client, this._opts);
            (0, logger_1.getLogger)().debug(`Checking result for AudioToVideoClient project ${createResponse.id}`);
            const result = yield projectsClient.checkResult({ id: createResponse.id }, Object.assign({ waitForCompletion,
                downloadOutputs,
                downloadDirectory }, createOpts));
            return result;
        });
    }
    /**
     * Audio-to-Video
     *
     * **What this API does**
     *
     * Create the same Audio To Video you can make in the browser, but programmatically, so you can automate it, run it at scale, or connect it to your own app or workflow.
     *
     * **Good for**
     * - Automation and batch processing
     * - Adding audio to video into apps, pipelines, or tools
     *
     * **How it works (3 steps)**
     * 1) Upload your inputs (video, image, or audio) with [Generate Upload URLs](https://docs.magichour.ai/api-reference/files/generate-asset-upload-urls) and copy the `file_path`.
     * 2) Send a request to create a audio to video job with the basic fields.
     * 3) Check the job status until it's `complete`, then download the result from `downloads`.
     *
     * **Key options**
     * - Inputs: usually a file, sometimes a YouTube link, depending on project type
     * - Resolution: free users are limited to 576px; higher plans unlock HD and larger sizes
     * - Extra fields: e.g. `face_swap_mode`, `start_seconds`/`end_seconds`, or a text prompt
     *
     * **Cost**
     * Credits are only charged for the frames that actually render. You'll see an estimate when the job is queued, and the final total after it's done.
     *
     * For detailed examples, see the [product page](https://magichour.ai/products/audio-to-video).
     *
     * POST /v1/audio-to-video
     */
    create(request, opts) {
        return this._client.makeRequest({
            method: "post",
            path: "/v1/audio-to-video",
            auth: ["bearerAuth"],
            contentType: "application/json",
            body: v1_audio_to_video_create_body_1.Schemas$V1AudioToVideoCreateBody.out.parse(request),
            responseSchema: v1_audio_to_video_create_response_1.Schemas$V1AudioToVideoCreateResponse.in,
            opts,
        });
    }
}
exports.AudioToVideoClient = AudioToVideoClient;
