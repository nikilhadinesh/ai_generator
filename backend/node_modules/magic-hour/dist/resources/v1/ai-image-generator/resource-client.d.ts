import { ApiPromise, CoreClient, CoreResourceClient, RequestOptions, ResourceClientOptions } from "make-api-request-js";
import { types } from "../../../index.js";
import { GenerateOptions, GenerateRequestType } from "../../../helpers/generate-type";
import * as requests from "../../../resources/v1/ai-image-generator/request-types";
type GenerateRequest = GenerateRequestType<requests.CreateRequest, {}>;
export declare class AiImageGeneratorClient extends CoreResourceClient {
    constructor(coreClient: CoreClient, opts: ResourceClientOptions);
    /**
     * AI Images
     *
     * Create an AI image
     *
     * This method provides a convenient way to create a request and automatically wait for completion and download outputs.
     *
     * @example
     * ```typescript
     * import { Client } from "../../../index.js";
     *
     * const client = new Client({ token: process.env["API_TOKEN"]!! });
     * const res = await client.v1.aiImageGenerator.generate(
     *   {
     *     imageCount: 1,
     *     name: "Ai Image image",
     *     orientation: "landscape",
     *     style: { prompt: "Cool image", tool: "ai-anime-generator" },
     *   },
     *   {
     *     waitForCompletion: true,
     *     downloadOutputs: true,
     *     downloadDirectory: ".",
     *   },
     * );
     * ```
     */
    generate(request: GenerateRequest, opts?: GenerateOptions): Promise<import("../image-projects/resource-client").V1ImageProjectsGetResponseWithDownloads>;
    /**
     * AI Image Generator
     *
     * Create an AI image with advanced model selection and quality controls.
     *
     * POST /v1/ai-image-generator
     */
    create(request: requests.CreateRequest, opts?: RequestOptions): ApiPromise<types.V1AiImageGeneratorCreateResponse>;
}
export {};
