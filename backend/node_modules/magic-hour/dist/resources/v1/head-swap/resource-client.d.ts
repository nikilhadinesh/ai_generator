import { ApiPromise, CoreClient, CoreResourceClient, RequestOptions, ResourceClientOptions } from "make-api-request-js";
import { GenerateOptions, GenerateRequestType } from "../../../helpers/generate-type";
import * as requests from "../../../resources/v1/head-swap/request-types";
import * as types from "../../../types";
type GenerateRequest = GenerateRequestType<requests.CreateRequest, {
    /**
     * Image that receives the swapped head. This value is either
     * - a direct URL to the image file
     * - a path to a local file
     *
     * Note: if the path begins with `api-assets`, it will be assumed to already be uploaded to Magic Hour's storage, and will not be uploaded again.
     */
    bodyFilePath: string;
    /**
     * Image of the head to place on the body. This value is either
     * - a direct URL to the image file
     * - a path to a local file
     *
     * Note: if the path begins with `api-assets`, it will be assumed to already be uploaded to Magic Hour's storage, and will not be uploaded again.
     */
    headFilePath: string;
}>;
export declare class HeadSwapClient extends CoreResourceClient {
    constructor(coreClient: CoreClient, opts: ResourceClientOptions);
    /**
     * Head Swap
     *
     * Swap a head onto a body image
     *
     * This method provides a convenient way to create a request and automatically wait for completion and download outputs.
     *
     * @example
     * ```typescript
     * import { Client } from "../../../index.js";
     *
     * const client = new Client({ token: process.env["API_TOKEN"]!! });
     * const res = await client.v1.headSwap.generate(
     *   {
     *     assets: {
     *       bodyFilePath: "/path/to/body.png",
     *       headFilePath: "/path/to/head.png",
     *     },
     *     maxResolution: 1024,
     *     name: "My Head Swap image",
     *   },
     *   {
     *     waitForCompletion: true,
     *     downloadOutputs: true,
     *     downloadDirectory: ".",
     *   },
     * );
     * ```
     */
    generate(request: GenerateRequest, opts?: GenerateOptions): Promise<import("../image-projects/resource-client").V1ImageProjectsGetResponseWithDownloads>;
    /**
     * Head Swap
     *
     * Swap a head onto a body image. Each image costs 10 credits. Output resolution depends on your subscription; you may set `max_resolution` lower than your plan maximum if desired.
     *
     * POST /v1/head-swap
     */
    create(request: requests.CreateRequest, opts?: RequestOptions): ApiPromise<types.V1HeadSwapCreateResponse>;
}
export {};
