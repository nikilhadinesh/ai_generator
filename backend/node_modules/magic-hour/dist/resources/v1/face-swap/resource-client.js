"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.FaceSwapClient = void 0;
const make_api_request_js_1 = require("make-api-request-js");
const logger_1 = require("../../../logger");
const files_1 = require("../../../resources/v1/files");
const video_projects_1 = require("../../../resources/v1/video-projects");
const v1_face_swap_create_body_1 = require("../../../types/v1-face-swap-create-body");
const v1_face_swap_create_response_1 = require("../../../types/v1-face-swap-create-response");
class FaceSwapClient extends make_api_request_js_1.CoreResourceClient {
    constructor(coreClient, opts) {
        super(coreClient, opts);
    }
    /**
     * Face Swap video
     *
     * Create a Face Swap video
     *
     * This method provides a convenient way to create a request and automatically wait for completion and download outputs.
     *
     * @example
     * ```typescript
     * import { Client } from "../../../index.js";
     *
     * const client = new Client({ token: process.env["API_TOKEN"]!! });
     * const res = await client.v1.faceSwap.generate(
     *   {
     *     assets: {
     *       faceMappings: [
     *         {
     *           newFace: "api-assets/id/1234.png",
     *           originalFace: "api-assets/id/0-0.png",
     *         },
     *       ],
     *       faceSwapMode: "all-faces",
     *       imageFilePath: "image/id/1234.png",
     *       videoFilePath: "/path/to/1234.mp4",
     *       videoSource: "file",
     *     },
     *     endSeconds: 15.0,
     *     name: "Face Swap video",
     *     startSeconds: 0.0,
     *     style: { version: "default" },
     *   },
     *   {
     *     waitForCompletion: true,
     *     downloadOutputs: true,
     *     downloadDirectory: ".",
     *   },
     * );
     * ```
     */
    generate(request_1) {
        return __awaiter(this, arguments, void 0, function* (request, opts = {}) {
            const { waitForCompletion = true, downloadOutputs = true, downloadDirectory = undefined } = opts, createOpts = __rest(opts, ["waitForCompletion", "downloadOutputs", "downloadDirectory"]);
            const fileClient = new files_1.FilesClient(this._client, this._opts);
            const _a = request.assets, { imageFilePath, videoFilePath, faceMappings } = _a, restAssets = __rest(_a, ["imageFilePath", "videoFilePath", "faceMappings"]);
            // Upload main files
            if (imageFilePath) {
                (0, logger_1.getLogger)().debug(`Uploading file ${imageFilePath} to Magic Hour's storage`);
            }
            if (videoFilePath) {
                (0, logger_1.getLogger)().debug(`Uploading file ${videoFilePath} to Magic Hour's storage`);
            }
            const [uploadedImageFilePath, uploadedVideoFilePath] = yield Promise.all([
                imageFilePath
                    ? fileClient.uploadFile(imageFilePath)
                    : Promise.resolve(imageFilePath),
                videoFilePath
                    ? fileClient.uploadFile(videoFilePath)
                    : Promise.resolve(videoFilePath),
            ]);
            if (imageFilePath) {
                (0, logger_1.getLogger)().info(`Uploaded file ${imageFilePath} to Magic Hour's storage as ${uploadedImageFilePath}`);
            }
            if (videoFilePath) {
                (0, logger_1.getLogger)().info(`Uploaded file ${videoFilePath} to Magic Hour's storage as ${uploadedVideoFilePath}`);
            }
            // Upload faceMappings newFace files if they exist
            let updatedFaceMappings = faceMappings;
            if (faceMappings && faceMappings.length > 0) {
                (0, logger_1.getLogger)().debug(`Uploading ${faceMappings.length} newFace files for face mappings`);
                const uploadPromises = faceMappings.map((mapping, index) => __awaiter(this, void 0, void 0, function* () {
                    const { newFace, originalFace } = mapping;
                    // Check if newFace needs to be uploaded (not already uploaded and not a URL)
                    if (newFace &&
                        !newFace.startsWith("api-assets/") &&
                        !newFace.startsWith("http")) {
                        (0, logger_1.getLogger)().debug(`Uploading newFace file ${newFace} for face mapping ${index}`);
                        const uploadedNewFace = yield fileClient.uploadFile(newFace);
                        (0, logger_1.getLogger)().info(`Uploaded newFace file ${newFace} as ${uploadedNewFace} for face mapping ${index}`);
                        return {
                            newFace: uploadedNewFace,
                            originalFace,
                        };
                    }
                    else {
                        return {
                            newFace,
                            originalFace,
                        };
                    }
                }));
                updatedFaceMappings = yield Promise.all(uploadPromises);
            }
            const createResponse = yield this.create(Object.assign(Object.assign({}, request), { assets: Object.assign(Object.assign(Object.assign({}, restAssets), { imageFilePath: imageFilePath ? uploadedImageFilePath : imageFilePath, videoFilePath: videoFilePath ? uploadedVideoFilePath : videoFilePath }), (updatedFaceMappings && { faceMappings: updatedFaceMappings })) }), createOpts);
            (0, logger_1.getLogger)().info(`Created FaceSwapClient project ${createResponse.id}`);
            const projectsClient = new video_projects_1.VideoProjectsClient(this._client, this._opts);
            (0, logger_1.getLogger)().debug(`Checking result for FaceSwapClient project ${createResponse.id}`);
            const result = yield projectsClient.checkResult({ id: createResponse.id }, Object.assign({ waitForCompletion,
                downloadOutputs,
                downloadDirectory }, createOpts));
            return result;
        });
    }
    /**
     * Face Swap Video
     *
     * **What this API does**
     *
     * Create the same Face Swap you can make in the browser, but programmatically, so you can automate it, run it at scale, or connect it to your own app or workflow.
     *
     * **Good for**
     * - Automation and batch processing
     * - Adding face swap into apps, pipelines, or tools
     *
     * **How it works (3 steps)**
     * 1) Upload your inputs (video, image, or audio) with [Generate Upload URLs](https://docs.magichour.ai/api-reference/files/generate-asset-upload-urls) and copy the `file_path`.
     * 2) Send a request to create a face swap job with the basic fields.
     * 3) Check the job status until it's `complete`, then download the result from `downloads`.
     *
     * **Key options**
     * - Inputs: usually a file, sometimes a YouTube link, depending on project type
     * - Resolution: free users are limited to 576px; higher plans unlock HD and larger sizes
     * - Extra fields: e.g. `face_swap_mode`, `start_seconds`/`end_seconds`, or a text prompt
     *
     * **Cost**
     * Credits are only charged for the frames that actually render. You'll see an estimate when the job is queued, and the final total after it's done.
     *
     * For detailed examples, see the [product page](https://magichour.ai/products/face-swap).
     *
     * POST /v1/face-swap
     */
    create(request, opts) {
        return this._client.makeRequest({
            method: "post",
            path: "/v1/face-swap",
            auth: ["bearerAuth"],
            contentType: "application/json",
            body: v1_face_swap_create_body_1.Schemas$V1FaceSwapCreateBody.out.parse(request),
            responseSchema: v1_face_swap_create_response_1.Schemas$V1FaceSwapCreateResponse.in,
            opts,
        });
    }
}
exports.FaceSwapClient = FaceSwapClient;
