import { ApiPromise, CoreClient, CoreResourceClient, RequestOptions, ResourceClientOptions } from "make-api-request-js";
import { types } from "../../../index.js";
import { GenerateOptions, GenerateRequestType } from "../../../helpers/generate-type";
import * as requests from "../../../resources/v1/ai-voice-cloner/request-types";
type GenerateRequest = GenerateRequestType<requests.CreateRequest, {
    /**
     * The audio used to clone the voice. This value is either
     * - a direct URL to the audio file
     * - a path to a local file
     *
     * Note: if the path begins with `api-assets`, it will be assumed to already be uploaded to Magic Hour's storage, and will not be uploaded again.
     */
    audioFilePath: string;
}>;
export declare class AiVoiceClonerClient extends CoreResourceClient {
    constructor(coreClient: CoreClient, opts: ResourceClientOptions);
    /**
     * AI Voice Cloner
     *
     * Clone a voice from an audio sample and generate speech.
     *
     * This method provides a convenient way to create a request and automatically wait for completion and download outputs.
     *
     * @example
     * ```typescript
     * import { Client } from "../../../index.js";
     *
     * const client = new Client({ token: process.env["API_TOKEN"]!! });
     * const res = await client.v1.aiVoiceCloner.generate(
     *   {
     *     assets: { audioFilePath: "/path/to/audio.mp3" },
     *     name: "Voice Cloner audio",
     *     style: { prompt: "Hello, how are you?" },
     *   },
     *   {
     *     waitForCompletion: true,
     *     downloadOutputs: true,
     *     downloadDirectory: ".",
     *   },
     * );
     * ```
     */
    generate(request: GenerateRequest, opts?: GenerateOptions): Promise<import("../image-projects/resource-client").V1ImageProjectsGetResponseWithDownloads>;
    /**
     * AI Voice Cloner
     *
     * Clone a voice from an audio sample and generate speech.
     * * Each character costs 0.1 credits.
     * * The cost is rounded up to the nearest whole number
     *
     * POST /v1/ai-voice-cloner
     */
    create(request: requests.CreateRequest, opts?: RequestOptions): ApiPromise<types.V1AiVoiceClonerCreateResponse>;
}
export {};
