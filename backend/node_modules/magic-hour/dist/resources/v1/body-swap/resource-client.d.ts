import { ApiPromise, CoreClient, CoreResourceClient, RequestOptions, ResourceClientOptions } from "make-api-request-js";
import { GenerateOptions, GenerateRequestType } from "../../../helpers/generate-type";
import * as requests from "../../../resources/v1/body-swap/request-types";
import * as types from "../../../types";
type GenerateRequest = GenerateRequestType<requests.CreateRequest, {
    /**
     * Image of the person to place into the scene. This value is either
     * - a direct URL to the image file
     * - a path to a local file
     *
     * Note: if the path begins with `api-assets`, it will be assumed to already be uploaded to Magic Hour's storage, and will not be uploaded again.
     */
    personFilePath: string;
    /**
     * Target scene image (background). This value is either
     * - a direct URL to the image file
     * - a path to a local file
     *
     * Note: if the path begins with `api-assets`, it will be assumed to already be uploaded to Magic Hour's storage, and will not be uploaded again.
     */
    sceneFilePath: string;
}>;
export declare class BodySwapClient extends CoreResourceClient {
    constructor(coreClient: CoreClient, opts: ResourceClientOptions);
    /**
     * Body Swap
     *
     * Swap a person into a scene image using Nano Banana 2. Credits depend on `resolution` (from 100 credits at 640px upward).
     *
     * This method provides a convenient way to create a request and automatically wait for completion and download outputs.
     *
     * @example
     * ```typescript
     * import { Client } from "../../../index.js";
     *
     * const client = new Client({ token: process.env["API_TOKEN"]!! });
     * const res = await client.v1.bodySwap.generate(
     *   {
     *     assets: {
     *       personFilePath: "/path/to/person.png",
     *       sceneFilePath: "/path/to/scene.png",
     *     },
     *     name: "My Body Swap image",
     *     resolution: "1k",
     *   },
     *   {
     *     waitForCompletion: true,
     *     downloadOutputs: true,
     *     downloadDirectory: ".",
     *   },
     * );
     * ```
     */
    generate(request: GenerateRequest, opts?: GenerateOptions): Promise<import("../image-projects/resource-client").V1ImageProjectsGetResponseWithDownloads>;
    /**
     * Body Swap
     *
     * Swap a person into a scene image using Nano Banana 2 Lite (640px/1k) or Nano Banana 2 (2k/4k). Credits depend on `resolution` (from 50 credits at 640px upward).
     *
     * POST /v1/body-swap
     */
    create(request: requests.CreateRequest, opts?: RequestOptions): ApiPromise<types.V1BodySwapCreateResponse>;
}
export {};
