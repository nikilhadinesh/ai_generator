"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.FaceSwapPhotoClient = void 0;
const make_api_request_js_1 = require("make-api-request-js");
const logger_1 = require("../../../logger");
const files_1 = require("../../../resources/v1/files");
const image_projects_1 = require("../../../resources/v1/image-projects");
const v1_face_swap_photo_create_body_1 = require("../../../types/v1-face-swap-photo-create-body");
const v1_face_swap_photo_create_response_1 = require("../../../types/v1-face-swap-photo-create-response");
class FaceSwapPhotoClient extends make_api_request_js_1.CoreResourceClient {
    constructor(coreClient, opts) {
        super(coreClient, opts);
    }
    /**
     * Face Swap Photo
     *
     * Create a face swap photo
     *
     * This method provides a convenient way to create a request and automatically wait for completion and download outputs.
     *
     * @example
     * ```typescript
     * import { Client } from "../../../index.js";
     *
     * const client = new Client({ token: process.env["API_TOKEN"]!! });
     * const res = await client.v1.faceSwapPhoto.generate(
     *   {
     *     assets: {
     *       faceMappings: [
     *         {
     *           newFace: "api-assets/id/1234.png",
     *           originalFace: "api-assets/id/0-0.png",
     *         },
     *       ],
     *       faceSwapMode: "all-faces",
     *       sourceFilePath: "/path/to/1234.png",
     *       targetFilePath: "/path/to/1234.png",
     *     },
     *     name: "Face Swap image",
     *   },
     *   {
     *     waitForCompletion: true,
     *     downloadOutputs: true,
     *     downloadDirectory: ".",
     *   },
     * );
     * ```
     */
    generate(request_1) {
        return __awaiter(this, arguments, void 0, function* (request, opts = {}) {
            const { waitForCompletion = true, downloadOutputs = true, downloadDirectory = undefined } = opts, createOpts = __rest(opts, ["waitForCompletion", "downloadOutputs", "downloadDirectory"]);
            const fileClient = new files_1.FilesClient(this._client, this._opts);
            const _a = request.assets, { sourceFilePath, targetFilePath, faceMappings } = _a, restAssets = __rest(_a, ["sourceFilePath", "targetFilePath", "faceMappings"]);
            // Upload main files
            if (sourceFilePath) {
                (0, logger_1.getLogger)().debug(`Uploading file ${sourceFilePath} to Magic Hour's storage`);
            }
            (0, logger_1.getLogger)().debug(`Uploading file ${targetFilePath} to Magic Hour's storage`);
            const [uploadedSourceFilePath, uploadedTargetFilePath] = yield Promise.all([
                sourceFilePath
                    ? fileClient.uploadFile(sourceFilePath)
                    : Promise.resolve(sourceFilePath),
                fileClient.uploadFile(targetFilePath),
            ]);
            if (sourceFilePath) {
                (0, logger_1.getLogger)().info(`Uploaded file ${sourceFilePath} to Magic Hour's storage as ${uploadedSourceFilePath}`);
            }
            (0, logger_1.getLogger)().info(`Uploaded file ${targetFilePath} to Magic Hour's storage as ${uploadedTargetFilePath}`);
            // Upload faceMappings newFace files if they exist
            let updatedFaceMappings = faceMappings;
            if (faceMappings && faceMappings.length > 0) {
                (0, logger_1.getLogger)().debug(`Uploading ${faceMappings.length} newFace files for face mappings`);
                const uploadPromises = faceMappings.map((mapping, index) => __awaiter(this, void 0, void 0, function* () {
                    const { newFace, originalFace } = mapping;
                    // Check if newFace needs to be uploaded (not already uploaded and not a URL)
                    if (newFace &&
                        !newFace.startsWith("api-assets/") &&
                        !newFace.startsWith("http")) {
                        (0, logger_1.getLogger)().debug(`Uploading newFace file ${newFace} for face mapping ${index}`);
                        const uploadedNewFace = yield fileClient.uploadFile(newFace);
                        (0, logger_1.getLogger)().info(`Uploaded newFace file ${newFace} as ${uploadedNewFace} for face mapping ${index}`);
                        return {
                            newFace: uploadedNewFace,
                            originalFace,
                        };
                    }
                    else {
                        return {
                            newFace,
                            originalFace,
                        };
                    }
                }));
                updatedFaceMappings = yield Promise.all(uploadPromises);
            }
            const createResponse = yield this.create(Object.assign(Object.assign({}, request), { assets: Object.assign(Object.assign(Object.assign({}, restAssets), { sourceFilePath: sourceFilePath
                        ? uploadedSourceFilePath
                        : sourceFilePath, targetFilePath: uploadedTargetFilePath }), (updatedFaceMappings && { faceMappings: updatedFaceMappings })) }), createOpts);
            (0, logger_1.getLogger)().info(`Created FaceSwapPhotoClient project ${createResponse.id}`);
            const projectsClient = new image_projects_1.ImageProjectsClient(this._client, this._opts);
            (0, logger_1.getLogger)().debug(`Checking result for FaceSwapPhotoClient project ${createResponse.id}`);
            const result = yield projectsClient.checkResult({ id: createResponse.id }, Object.assign({ waitForCompletion,
                downloadOutputs,
                downloadDirectory }, createOpts));
            return result;
        });
    }
    /**
     * Face Swap Photo
     *
     * Create a face swap photo. Each photo costs 10 credits. The height/width of the output image depends on your subscription. Please refer to our [pricing](https://magichour.ai/pricing) page for more details
     *
     * POST /v1/face-swap-photo
     */
    create(request, opts) {
        return this._client.makeRequest({
            method: "post",
            path: "/v1/face-swap-photo",
            auth: ["bearerAuth"],
            contentType: "application/json",
            body: v1_face_swap_photo_create_body_1.Schemas$V1FaceSwapPhotoCreateBody.out.parse(request),
            responseSchema: v1_face_swap_photo_create_response_1.Schemas$V1FaceSwapPhotoCreateResponse.in,
            opts,
        });
    }
}
exports.FaceSwapPhotoClient = FaceSwapPhotoClient;
