import { ApiPromise, CoreClient, CoreResourceClient, RequestOptions, ResourceClientOptions } from "make-api-request-js";
import { types } from "../../../index.js";
import { GenerateOptions, GenerateRequestType } from "../../../helpers/generate-type";
import * as requests from "../../../resources/v1/animation/request-types";
type GenerateRequest = GenerateRequestType<requests.CreateRequest, {
    /**
     * The path of the input audio. This value is either
     * - a direct URL to the image file
     * - a path to a local file
     *
     * Note: if the path begins with `api-assets`, it will be assumed to already be uploaded to Magic Hour's storage, and will not be uploaded again.
     */
    audioFilePath?: string | undefined;
    /**
     * An initial image to use a the first frame of the video. This value is either
     * - a direct URL to the image file
     * - a path to a local file
     *
     * Note: if the path begins with `api-assets`, it will be assumed to already be uploaded to Magic Hour's storage, and will not be uploaded again.
     */
    imageFilePath?: string | undefined;
}>;
export declare class AnimationClient extends CoreResourceClient {
    constructor(coreClient: CoreClient, opts: ResourceClientOptions);
    /**
     * Animation
     *
     * Create a Animation video
     *
     * This method provides a convenient way to create a request and automatically wait for completion and download outputs.
     *
     * @example
     * ```typescript
     * import { Client } from "../../../index.js";
     *
     * const client = new Client({ token: process.env["API_TOKEN"]!! });
     * const res = await client.v1.animation.generate(
     *   {
     *     assets: {
     *       audioFilePath: "/path/to/1234.mp3",
     *       audioSource: "file",
     *       imageFilePath: "/path/to/1234.png",
     *     },
     *     endSeconds: 15.0,
     *     fps: 12.0,
     *     height: 960,
     *     name: "Animation video",
     *     style: {
     *       artStyle: "Painterly Illustration",
     *       cameraEffect: "Simple Zoom In",
     *       prompt: "Cyberpunk city",
     *       promptType: "custom",
     *       transitionSpeed: 5,
     *     },
     *     width: 512,
     *   },
     *   {
     *     waitForCompletion: true,
     *     downloadOutputs: true,
     *     downloadDirectory: ".",
     *   },
     * );
     * ```
     */
    generate(request: GenerateRequest, opts?: GenerateOptions): Promise<import("../video-projects/resource-client").V1VideoProjectsGetResponseWithDownloads>;
    /**
     * Animation
     *
     * Create a Animation video. The estimated frame cost is calculated based on the `fps` and `end_seconds` input.
     *
     * POST /v1/animation
     */
    create(request: requests.CreateRequest, opts?: RequestOptions): ApiPromise<types.V1AnimationCreateResponse>;
}
export {};
