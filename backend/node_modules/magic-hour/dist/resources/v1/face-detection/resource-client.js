"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.FaceDetectionClient = void 0;
const make_api_request_js_1 = require("make-api-request-js");
const download_1 = require("../../../helpers/download");
const sleep_1 = require("../../../helpers/sleep");
const logger_1 = require("../../../logger");
const files_1 = require("../../../resources/v1/files");
const v1_face_detection_create_body_1 = require("../../../types/v1-face-detection-create-body");
const v1_face_detection_create_response_1 = require("../../../types/v1-face-detection-create-response");
const v1_face_detection_get_response_1 = require("../../../types/v1-face-detection-get-response");
class FaceDetectionClient extends make_api_request_js_1.CoreResourceClient {
    constructor(coreClient, opts) {
        super(coreClient, opts);
    }
    /**
     * Face Detection
     *
     * Detect faces in an image or video
     *
     * This method provides a convenient way to create a request and automatically wait for completion and download outputs.
     *
     */
    generate(request_1) {
        return __awaiter(this, arguments, void 0, function* (request, opts = {}) {
            const { waitForCompletion = true, downloadOutputs = true, downloadDirectory = undefined } = opts, requestOpts = __rest(opts, ["waitForCompletion", "downloadOutputs", "downloadDirectory"]);
            const fileClient = new files_1.FilesClient(this._client, this._opts);
            const _a = request.assets, { targetFilePath } = _a, restAssets = __rest(_a, ["targetFilePath"]);
            const [uploadedTargetFilePath] = yield Promise.all([
                fileClient.uploadFile(targetFilePath),
            ]);
            const createResponse = yield this.create(Object.assign(Object.assign({}, request), { assets: Object.assign(Object.assign({}, restAssets), { targetFilePath: uploadedTargetFilePath }) }), requestOpts);
            let apiResponse = yield this.get({ id: createResponse.id }, requestOpts);
            if (!waitForCompletion) {
                return Object.assign({}, apiResponse);
            }
            const pollInterval = parseFloat(process.env["MAGIC_HOUR_POLL_INTERVAL"] || "0.5");
            (0, logger_1.getLogger)().debug(`Polling face detection ${createResponse.id} every ${pollInterval} seconds`);
            while (!["complete", "error", "canceled"].includes(apiResponse.status)) {
                yield (0, sleep_1.sleep)(pollInterval * 1000); // Convert seconds to milliseconds
                apiResponse = yield this.get({ id: createResponse.id }, requestOpts);
                (0, logger_1.getLogger)().info(`Face detection ${createResponse.id} status: ${apiResponse.status}, waiting for ${pollInterval} seconds and checking again`);
            }
            if (apiResponse.status !== "complete") {
                const message = `Face detection ${apiResponse.id} has status ${apiResponse.status}`;
                if (apiResponse.status === "error") {
                    (0, logger_1.getLogger)().error(message);
                }
                else {
                    (0, logger_1.getLogger)().info(message);
                }
                return Object.assign({}, apiResponse);
            }
            if (!downloadOutputs) {
                (0, logger_1.getLogger)().info(`Download outputs is disabled. Returning face detection ${apiResponse.id} with status ${apiResponse.status}`);
                return Object.assign({}, apiResponse);
            }
            (0, logger_1.getLogger)().debug(`Downloading outputs for face detection ${apiResponse.id} to ${downloadDirectory !== null && downloadDirectory !== void 0 ? downloadDirectory : "current directory"}`);
            const downloadedPaths = yield (0, download_1.downloadFiles)(apiResponse.faces.map((f) => ({
                url: f.url,
                expiresAt: "ignore",
            })), downloadDirectory);
            (0, logger_1.getLogger)().info(`Downloaded outputs for face detection ${apiResponse.id} to ${downloadedPaths}`);
            return Object.assign(Object.assign({}, apiResponse), { downloadedPaths });
        });
    }
    /**
     * Get face detection details
     *
     * Get the details of a face detection task.
     *
     * Use this API to get the list of faces detected in the image or video to use in the [face swap photo](https://docs.magichour.ai/api-reference/image-projects/face-swap-photo) or [face swap video](https://docs.magichour.ai/api-reference/video-projects/face-swap-video) API calls for multi-face swaps.
     *
     * GET /v1/face-detection/{id}
     */
    get(request, opts) {
        return this._client.makeRequest({
            method: "get",
            path: `/v1/face-detection/${request.id}`,
            auth: ["bearerAuth"],
            responseSchema: v1_face_detection_get_response_1.Schemas$V1FaceDetectionGetResponse.in,
            opts,
        });
    }
    /**
     * Face Detection
     *
     * Detect faces in an image or video.
     *
     * Use this API to get the list of faces detected in the image or video to use in the [face swap photo](https://docs.magichour.ai/api-reference/image-projects/face-swap-photo) or [face swap video](https://docs.magichour.ai/api-reference/video-projects/face-swap-video) API calls for multi-face swaps.
     *
     * Note: Face detection is free to use for the near future. Pricing may change in the future.
     *
     * POST /v1/face-detection
     */
    create(request, opts) {
        return this._client.makeRequest({
            method: "post",
            path: "/v1/face-detection",
            auth: ["bearerAuth"],
            contentType: "application/json",
            body: v1_face_detection_create_body_1.Schemas$V1FaceDetectionCreateBody.out.parse(request),
            responseSchema: v1_face_detection_create_response_1.Schemas$V1FaceDetectionCreateResponse.in,
            opts,
        });
    }
}
exports.FaceDetectionClient = FaceDetectionClient;
