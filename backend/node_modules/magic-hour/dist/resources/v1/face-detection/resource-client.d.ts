import { ApiPromise, CoreClient, CoreResourceClient, RequestOptions, ResourceClientOptions } from "make-api-request-js";
import { types } from "../../../index.js";
import { GenerateOptions, GenerateRequestType } from "../../../helpers/generate-type";
import * as requests from "../../../resources/v1/face-detection/request-types";
type GenerateRequest = GenerateRequestType<requests.CreateRequest, {
    /** This is the image or video where the face will be detected. This value is either
     * - a direct URL to the image file
     * - a path to a local file
     *
     * Note: if the path begins with `api-assets`, it will be assumed to already be uploaded to Magic Hour's storage, and will not be uploaded again. */
    targetFilePath: string;
}>;
export declare class FaceDetectionClient extends CoreResourceClient {
    constructor(coreClient: CoreClient, opts: ResourceClientOptions);
    /**
     * Face Detection
     *
     * Detect faces in an image or video
     *
     * This method provides a convenient way to create a request and automatically wait for completion and download outputs.
     *
     */
    generate(request: GenerateRequest, opts?: GenerateOptions): Promise<{
        creditsCharged: number;
        faces: types.V1FaceDetectionGetResponseFacesItem[];
        id: string;
        status: "complete" | "error" | "queued" | "rendering";
    } | {
        downloadedPaths: string[];
        creditsCharged: number;
        faces: types.V1FaceDetectionGetResponseFacesItem[];
        id: string;
        status: "complete" | "error" | "queued" | "rendering";
    }>;
    /**
     * Get face detection details
     *
     * Get the details of a face detection task.
     *
     * Use this API to get the list of faces detected in the image or video to use in the [face swap photo](https://docs.magichour.ai/api-reference/image-projects/face-swap-photo) or [face swap video](https://docs.magichour.ai/api-reference/video-projects/face-swap-video) API calls for multi-face swaps.
     *
     * GET /v1/face-detection/{id}
     */
    get(request: requests.GetRequest, opts?: RequestOptions): ApiPromise<types.V1FaceDetectionGetResponse>;
    /**
     * Face Detection
     *
     * Detect faces in an image or video.
     *
     * Use this API to get the list of faces detected in the image or video to use in the [face swap photo](https://docs.magichour.ai/api-reference/image-projects/face-swap-photo) or [face swap video](https://docs.magichour.ai/api-reference/video-projects/face-swap-video) API calls for multi-face swaps.
     *
     * Note: Face detection is free to use for the near future. Pricing may change in the future.
     *
     * POST /v1/face-detection
     */
    create(request: requests.CreateRequest, opts?: RequestOptions): ApiPromise<types.V1FaceDetectionCreateResponse>;
}
export {};
