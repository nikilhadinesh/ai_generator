import { ApiPromise, CoreClient, CoreResourceClient, RequestOptions, ResourceClientOptions } from "make-api-request-js";
import { types } from "../../../index.js";
import { GenerateOptions, GenerateRequestType } from "../../../helpers/generate-type";
import * as requests from "../../../resources/v1/ai-face-editor/request-types";
type GenerateRequest = GenerateRequestType<requests.CreateRequest, {
    /**
     * This is the image whose face will be edited. This value is either
     * - a direct URL to the image file
     * - a path to a local file
     *
     * Note: if the path begins with `api-assets`, it will be assumed to already be uploaded to Magic Hour's storage, and will not be uploaded again.
     */
    imageFilePath: string;
}>;
export declare class AiFaceEditorClient extends CoreResourceClient {
    constructor(coreClient: CoreClient, opts: ResourceClientOptions);
    /**
     * AI Face Editor
     *
     * Edit facial features of an image using AI
     *
     * This method provides a convenient way to create a request and automatically wait for completion and download outputs.
     *
     * @example
     * ```typescript
     * import { Client } from "../../../index.js";
     *
     * const client = new Client({ token: process.env["API_TOKEN"]!! });
     * const res = await client.v1.aiFaceEditor.generate(
     *   {
     *     assets: { imageFilePath: "/path/to/1234.png" },
     *     name: "Face Editor image",
     *     style: {
     *       enhanceFace: false,
     *       eyeGazeHorizontal: 0.0,
     *       eyeGazeVertical: 0.0,
     *       eyeOpenRatio: 0.0,
     *       eyebrowDirection: 0.0,
     *       headPitch: 0.0,
     *       headRoll: 0.0,
     *       headYaw: 0.0,
     *       lipOpenRatio: 0.0,
     *       mouthGrim: 0.0,
     *       mouthPositionHorizontal: 0.0,
     *       mouthPositionVertical: 0.0,
     *       mouthPout: 0.0,
     *       mouthPurse: 0.0,
     *       mouthSmile: 0.0,
     *     },
     *   },
     *   {
     *     waitForCompletion: true,
     *     downloadOutputs: true,
     *     downloadDirectory: ".",
     *   },
     * );
     * ```
     */
    generate(request: GenerateRequest, opts?: GenerateOptions): Promise<import("../image-projects/resource-client").V1ImageProjectsGetResponseWithDownloads>;
    /**
     * AI Face Editor
     *
     * Edit facial features of an image using AI. Each edit costs 1 frame. The height/width of the output image depends on your subscription. Please refer to our [pricing](https://magichour.ai/pricing) page for more details
     *
     * POST /v1/ai-face-editor
     */
    create(request: requests.CreateRequest, opts?: RequestOptions): ApiPromise<types.V1AiFaceEditorCreateResponse>;
}
export {};
