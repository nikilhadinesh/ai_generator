"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.AutoSubtitleGeneratorClient = void 0;
const make_api_request_js_1 = require("make-api-request-js");
const logger_1 = require("../../../logger");
const files_1 = require("../../../resources/v1/files");
const video_projects_1 = require("../../../resources/v1/video-projects");
const v1_auto_subtitle_generator_create_body_1 = require("../../../types/v1-auto-subtitle-generator-create-body");
const v1_auto_subtitle_generator_create_response_1 = require("../../../types/v1-auto-subtitle-generator-create-response");
class AutoSubtitleGeneratorClient extends make_api_request_js_1.CoreResourceClient {
    constructor(coreClient, opts) {
        super(coreClient, opts);
    }
    /**
     * Auto Subtitle Generator
     *
     * Automatically generate subtitles for your video in multiple languages
     *
     * This method provides a convenient way to create a request and automatically wait for completion and download outputs.
     *
     * @example
     * ```typescript
     * import { Client } from "../../../index.js";
     *
     * const client = new Client({ token: process.env["API_TOKEN"]!! });
     * const res = await client.v1.autoSubtitleGenerator.generate(
     *   {
     *     assets: { videoFilePath: "/path/to/1234.mp4" },
     *     endSeconds: 15.0,
     *     name: "Auto Subtitle video",
     *     startSeconds: 0.0,
     *     style: {},
     *   },
     *   {
     *     waitForCompletion: true,
     *     downloadOutputs: true,
     *     downloadDirectory: ".",
     *   },
     * );
     * ```
     */
    generate(request_1) {
        return __awaiter(this, arguments, void 0, function* (request, opts = {}) {
            const { waitForCompletion = true, downloadOutputs = true, downloadDirectory = undefined } = opts, createOpts = __rest(opts, ["waitForCompletion", "downloadOutputs", "downloadDirectory"]);
            const fileClient = new files_1.FilesClient(this._client, this._opts);
            const _a = request.assets, { videoFilePath } = _a, restAssets = __rest(_a, ["videoFilePath"]);
            (0, logger_1.getLogger)().debug(`Uploading file ${videoFilePath} to Magic Hour's storage`);
            const [uploadedVideoFilePath] = yield Promise.all([
                fileClient.uploadFile(videoFilePath),
            ]);
            (0, logger_1.getLogger)().info(`Uploaded file ${videoFilePath} to Magic Hour's storage as ${uploadedVideoFilePath}`);
            const createResponse = yield this.create(Object.assign(Object.assign({}, request), { assets: Object.assign(Object.assign({}, restAssets), { videoFilePath: uploadedVideoFilePath }) }), createOpts);
            (0, logger_1.getLogger)().info(`Created AutoSubtitleGeneratorClient project ${createResponse.id}`);
            const projectsClient = new video_projects_1.VideoProjectsClient(this._client, this._opts);
            (0, logger_1.getLogger)().debug(`Checking result for AutoSubtitleGeneratorClient project ${createResponse.id}`);
            const result = yield projectsClient.checkResult({ id: createResponse.id }, Object.assign({ waitForCompletion,
                downloadOutputs,
                downloadDirectory }, createOpts));
            return result;
        });
    }
    /**
     * Auto Subtitle Generator
     *
     * Automatically generate subtitles for your video in multiple languages.
     *
     * POST /v1/auto-subtitle-generator
     */
    create(request, opts) {
        return this._client.makeRequest({
            method: "post",
            path: "/v1/auto-subtitle-generator",
            auth: ["bearerAuth"],
            contentType: "application/json",
            body: v1_auto_subtitle_generator_create_body_1.Schemas$V1AutoSubtitleGeneratorCreateBody.out.parse(request),
            responseSchema: v1_auto_subtitle_generator_create_response_1.Schemas$V1AutoSubtitleGeneratorCreateResponse.in,
            opts,
        });
    }
}
exports.AutoSubtitleGeneratorClient = AutoSubtitleGeneratorClient;
