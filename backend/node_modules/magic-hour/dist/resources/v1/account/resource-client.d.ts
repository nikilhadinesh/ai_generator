import { ApiPromise, CoreClient, CoreResourceClient, RequestOptions, ResourceClientOptions } from "make-api-request-js";
import * as types from "../../../types";
export declare class AccountClient extends CoreResourceClient {
    constructor(coreClient: CoreClient, opts: ResourceClientOptions);
    /**
     * Get account details
     *
     * Get the current credit balance and subscription details of the account that owns the API key.
     *
     * GET /v1/account
     */
    list(opts?: RequestOptions): ApiPromise<types.V1AccountListResponse>;
}
